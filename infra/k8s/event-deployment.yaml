apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: medibox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
        - name: event-service
          image: medibox/event-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          command: ["sh", "-c"]
          args:
            - alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000
          envFrom:
            - configMapRef:
                name: medibox-config
            - secretRef:
                name: medibox-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
